name: CD - Ong.Api.Campaigns

on:
  workflow_run:
    workflows: ["CI - Ong.Api.Campaigns"]
    branches: [main]
    types: [completed]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/ong-api-campaigns

jobs:
  docker-build-push:
    name: 🐳 Docker Build & Push
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: 🔐 Login GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 🏷️ Metadados
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=sha-
            type=raw,value=latest

      - name: 🚀 Build & Push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: src/Ong.Api.Campaigns/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  # ---------------------------------------------------------------
  # Deploy — comentado para fins acadêmicos (sem servidor real).
  # Para habilitar, descomente o job abaixo e configure as secrets:
  #   SSH_PRIVATE_KEY, DEPLOY_HOST, DEPLOY_USER,
  #   DEPLOY_COMPOSE_DIR, DEPLOY_URL
  # ---------------------------------------------------------------
  # deploy:
  #   name: 🚢 Deploy
  #   runs-on: ubuntu-latest
  #   needs: docker-build-push
  #   environment:
  #     name: production
  #     url: ${{ secrets.DEPLOY_URL }}
  #
  #   steps:
  #     - name: 🔑 Configurar SSH
  #       uses: webfactory/ssh-agent@v0.9.0
  #       with:
  #         ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
  #
  #     - name: 🚀 Atualizar container no servidor
  #       env:
  #         HOST: ${{ secrets.DEPLOY_HOST }}
  #         USER: ${{ secrets.DEPLOY_USER }}
  #         IMAGE: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
  #         COMPOSE_DIR: ${{ secrets.DEPLOY_COMPOSE_DIR }}
  #       run: |
  #         ssh -o StrictHostKeyChecking=no "$USER@$HOST" \
  #           "cd $COMPOSE_DIR && \
  #            docker compose pull ong-api-campaigns && \
  #            docker compose up -d --no-deps ong-api-campaigns && \
  #            docker image prune -f"
